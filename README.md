# Dancehall Telegram Bot

Готовый Telegram-бот для каталога dancehall-видео на `aiogram 3` + `sqlite`.

## Что умеет

- Добавление видео (Telegram файл или URL) с валидацией.
- Защита от дублей:
  - по `file_unique_id`;
  - по нормализованному URL;
  - обработка дубликата названия (заменить/копия/переименовать).
- Добавление категорий с фиксированными вариантами (множественный выбор от 1 до 7):
  - Вайны;
  - Волны;
  - Тряски;
  - Передвижения;
  - Easy;
  - Hard;
  - Другое (с ручным вводом значения).
- Поиск по:
  - категории;
  - названию.
- Пагинация по 10 элементов.
- Карточка видео с действиями:
  - скачать;
  - добавить/убрать из избранного;
  - редактировать;
  - удалить.
- Избранное хранится на пользователя (`user_id <-> video_id`).
- Безопасное удаление с подтверждением и предпросмотром карточки.
- Кнопки управления сценариями: `⬅️ Назад`, `❌ Отмена`, `🏠 В меню`.

## Технологии

- Python 3.11+
- aiogram 3
- sqlite3 (встроенная БД)

## Быстрый старт

1. Создайте бота через `@BotFather` и получите токен.
2. Создайте `.env` файл:

```env
BOT_TOKEN=ваш_токен
```

3. Установите зависимости:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

4. Запустите бота:

```bash
python bot.py
```

База `dancehall.db` создается автоматически при первом запуске.

## Структура

- `bot.py` — все FSM-сценарии, хендлеры и работа с БД.
- `storage.py` — модели хранения и SQL-операции.
- `requirements.txt` — зависимости проекта.

## Поведение целостности данных

- Удаление видео удаляет связи с категориями и избранным (через `ON DELETE CASCADE`).
- Удаление/изменение категорий не удаляет сами видео.
- Дубликаты видео не создаются.
